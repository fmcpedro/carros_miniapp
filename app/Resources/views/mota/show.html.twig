{% extends 'base.html.twig' %}

{% block body %}
    <h1>Mota</h1>

    <table>
        <tbody>
            <tr>
                <th>Peso</th>
                <td>{{ mota.peso }}</td>
            </tr>
            <tr>
                <th>Numrodas</th>
                <td>{{ mota.numerorodas }}</td>
            </tr>
                <!-- <tr>
            <th>Id</th>
                <td>{{ mota.id }}</td>
            </tr> -->
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('mota_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('mota_edit', { 'id': mota.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
