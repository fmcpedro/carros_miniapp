{% extends 'base.html.twig' %}

{% block body %}
    <h1>Carro edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
        
    {{ form_end(edit_form) }}
    
<img src="{{ vich_uploader_asset(carro, 'imageFile') }}" alt="{{ carro.imageFile }}" />
    <ul>
        <li>
            
           <!-- <a href="{{ path('carro_index') }}">Back to the list</a> -->
            <a href="{{ path('user_list_carros') }}">Back to your car list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete Car">
                
            {{ form_end(delete_form) }}
            
        </li>
    </ul>
{% endblock %}
