{% extends 'base.html.twig' %}

{% block body %}
    <h1>Numeroroda</h1>

    <table>
        <tbody>
            <tr>
                <th>Numerorodascol</th>
                <td>{{ numerorodas.numerorodascol }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ numerorodas.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('numerorodas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('numerorodas_edit', { 'id': numerorodas.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
